# Default development/CI dependency entry point.
#
# Direct application requirements live in requirements/production.in. SparCL
# is intentionally separate because its Python 3.12 pandas metadata conflicts
# with the production LSDB stack; see requirements/optional-sparcl.in.
-r requirements/production.in
