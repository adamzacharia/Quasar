# Core
setuptools<75
numpy
pandas
scipy
matplotlib
pillow
plotly
requests
python-dotenv
rich
humanize

# Astronomy
astropy>=6.1.0
astroquery
astropy-healpix
reproject
spectral-cube
pyvo
alminer
# NOIRLab Astro Data Lab: the connector (integrations/datalab_client.py) talks to
# the query-manager REST API directly via `requests` (already a dependency), so it
# runs on Python 3.13. The official `astro-datalab` client is OPTIONAL and only
# installs on Python <3.12 — uncomment if you prefer dl.queryClient on a 3.11 env.
# astro-datalab; python_version < "3.12"
altair==4.2.2
legacy-cgi>=2.6; python_version >= "3.13"
lightkurve>=2.4
psrqpy>=1.2

# AI / LLM
openai>=2.26.0
anthropic>=0.25.0,<1.0.0
google-genai>=1.0.0,<2.0.0
tavily-python>=0.3.0,<1.0.0

# RAG — pinned to compatible major.minor, loose on patch
langchain>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langchain-openai>=0.3.0,<0.4.0
qdrant-client>=1.7.0,<2.0.0
langchain-pymupdf4llm>=0.2.0,<1.0.0
tiktoken>=0.7.0,<1.0.0
# BM25 keyword scoring for hybrid RAG rerank (services/rag_service._hybrid_rerank).
# Import is guarded, but without this dep hybrid search silently degrades to
# semantic-only — declaring it makes the documented hybrid behaviour real. (C6)
rank_bm25>=0.2.2,<1.0.0

# Web / API
fastapi
uvicorn
python-multipart
aiofiles
aiohttp
httpx
beautifulsoup4

# Auth
google-auth
PyJWT
cryptography

# Database
libsql-client

# Task Queue
celery>=5.4.0,<6.0.0

# Monitoring
loguru>=0.7.0
rollbar>=0.16.0
langfuse>=2.0.0,<3.0.0

# Dev / Misc
GitPython
Jinja2
arxiv
diskcache
h5py
imageio
networkx

# Multimodal Universe / LSDB (HATS catalogs on Hugging Face). Lazy-imported by
# services/mmu_hats.py; Quasar boots fine without them, and the MMU tools then
# report "unavailable" instead of crashing.
lsdb==0.9.2
huggingface_hub

# NOIRLab SparCL spectra; lazy-imported by services/sparcl_spectra.py.
sparclclient
