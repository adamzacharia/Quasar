# Managed by Ansible (deploy/ansible) — do not hand-edit on the server.
# Edit group_vars/all.yml (config) or group_vars/vault.yml (secrets) and re-run
# the playbook. Loaded by api/bootstrap.py from the repo root.
{% for k, v in (quasar_env | combine(quasar_env_secrets | default({}))) | dictsort %}
{% if v is not none and (v | string | length) > 0 %}
{{ k }}={{ v }}
{% endif %}
{% endfor %}
