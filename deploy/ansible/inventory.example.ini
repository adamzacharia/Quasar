# Copy to inventory.ini and fill in the VM's public IP / hostname once TACC
# hands it over. inventory.ini is git-ignored (may contain host details).
[quasar]
quasar-vm ansible_host=REPLACE.WITH.VM.IP ansible_user=REPLACE_SSH_USER

[quasar:vars]
ansible_python_interpreter=auto_silent
