# Quasar Tool Registry

> **Auto-generated — do not edit by hand.** Regenerate with `python scripts/gen_tool_registry_doc.py`.  
> This file is the single source of truth for the tool count; CI fails if it drifts from the code (`python scripts/gen_tool_registry_doc.py --check`).

Quasar registers **140 tools** across **6 categories** on the native in-process path (per-user MCP and custom tools are added at runtime and are not listed here).

| Category | Tools |
|---|---|
| `analysis` | 24 |
| `archive` | 27 |
| `datalab` | 26 |
| `general` | 44 |
| `literature` | 16 |
| `mmu_hats` | 3 |
| **Total** | **140** |

## `analysis` (24)

| Tool | Parameters | Description |
|---|---|---|
| `calculate_alma_sensitivity` | `band`:integer\*, `bandwidth_ghz`:number, `t_integration_s`:number, `n_antennas`:integer, `n_polarizations`:integer, `channel_width_khz`:number, `pwv_mm`:number | Estimate ALMA continuum and spectral line sensitivity using the radiometer equation. Returns noise level in mJy/beam and uJy/beam for given band, bandwidth, and integration time. Includes Tsys scaling for weather (PWV). Use when the user asks about ALMA sensitivity, noise levels, or integration time estimates. |
| `calculate_beam` | `frequency_ghz`:number\*, `max_baseline_m`:number, `array_config`:string | Calculate the synthesized beam size for a radio interferometer given the maximum baseline and observing frequency. For ALMA, you can specify an array configuration name (C-1 through C-10) instead of a raw baseline length. Returns beam size in arcsec and milliarcsec. |
| `calculate_redshift` | `z`:number\* | Compute cosmological quantities for a given redshift z using Planck18 cosmology. Returns luminosity distance, angular diameter distance, comoving distance, lookback time, age of the universe at that epoch, and the physical scale (kpc per arcsecond). Use for any question about distances, ages, or scales at a given redshift. |
| `compute_moment_map` | `url`:string\*, `order`:integer, `title`:string, `colormap`:string, `freq_min_ghz`:number, `freq_max_ghz`:number | Download a FITS spectral cube and compute a moment map. Moment 0 = integrated intensity (total emission). Moment 1 = velocity field (mean velocity). Moment 2 = velocity dispersion (turbulence). Use this for ALMA cubes when the user asks about emission maps, velocity fields, or line intensity maps. |
| `convert_coordinates` | `ra`:number, `dec`:number, `l`:number, `b`:number, `input_frame`:string, `output_frame`:string | Convert sky coordinates between ICRS (RA/Dec), Galactic (l/b), Ecliptic (lon/lat), FK5 (J2000), and FK4 (B1950) frames. Returns the position in ALL frames at once. Use for coordinate transformations, epoch precession, or when the user gives Galactic coordinates and needs RA/Dec. |
| `extract_spectrum` | `url`:string\*, `ra_deg`:number, `dec_deg`:number, `x_pixel`:integer, `y_pixel`:integer, `title`:string | Download a FITS spectral cube and extract a 1D spectrum at a given sky position (RA/Dec) or pixel coordinate. If no position is given, extracts at the peak emission pixel. The spectrum is plotted as flux vs frequency/velocity and displayed inline. |
| `fit_spectral_line` | `url`:string\*, `ra_deg`:number, `dec_deg`:number, `x_pixel`:integer, `y_pixel`:integer, `title`:string | Download a FITS spectral cube, extract a 1D spectrum at a given position, and fit a Gaussian profile to the strongest line. Returns peak flux, FWHM (in frequency and velocity), center frequency, and integrated flux. The fit is overlaid on the spectrum plot. |
| `generate_finding_chart` | `target`:string, `ra`:number, `dec`:number, `survey`:string, `fov_arcmin`:number, `title`:string | Generate a publication-quality finding chart for a target. Creates a DSS2 or 2MASS image with WCS axes, a target crosshair marker, N/E compass arrows, and an angular scale bar. Use when the user needs a finding chart for observations or proposals. |
| `generate_jupyter_notebook` | `title`:string\*, `steps`:array\* | Generate a runnable Jupyter Notebook (.ipynb) for data analysis workflows. Use this when the user asks for code to analyze data, make maps, or perform reductions. |
| `monitor_add_target` | `name`:string\*, `target_name`:string, `ra`:number, `dec`:number, `radius_arcsec`:number, `note`:string | Add a sky position to the standing ZTF-alert watchlist; Quasar remembers it across sessions and reports only NEW alerts on each check. |
| `monitor_check_now` | `target_id`:integer | Check the watchlist (or one target) against ALeRCE/ZTF NOW and report only alerts that are new since the previous check. |
| `monitor_list_targets` | — | List the standing sky-monitor watchlist with hit counts and last-checked times. |
| `monitor_remove_target` | `target_id`:integer\* | Remove a sky-monitor watchlist entry (and its recorded alerts) by id. |
| `ned_sed_plot` | `target_name`:string\* | Plot a literature SED from NED photometry for a named target. |
| `overlay_archive_images` | `region`:string\*, `ra_deg`:number, `dec_deg`:number, `base_archive`:string, `base_collection`:string, `contour_archive`:string, `radius_arcmin`:number, `max_product_mb`:number | End-to-end archive image overlay workflow. Queries MAST/JWST for a background FITS image and ALMA/DataLink for contour FITS near a named region, WCS-aligns them, and renders a PNG. Use for requests like 'Overlay ALMA contours on JWST image for HUDF'. |
| `overlay_fits_images` | `base_url`:string\*, `contour_url`:string\*, `base_label`:string, `contour_label`:string, `base_cmap`:string, `contour_levels`:integer | Download two FITS files and create an overlay composite: one rendered as a colorscale background, the other as contours on top. Uses WCS reprojection to align them. Perfect for showing ALMA contours on JWST/HST colorscale images. |
| `period_search` | `source`:string\*, `identifier`:string\*, `mission`:string, `min_period_d`:number, `max_period_d`:number, `fid`:integer, `index`:integer | Lomb-Scargle period search plus phase-folded plot on a TESS/Kepler light curve or a ZTF object by ALeRCE oid; returns best period, FAP, and top alternatives. |
| `plot_space_lightcurve` | `target`:string\*, `mission`:string, `index`:integer | Download and plot a TESS/Kepler/K2 light curve for a target. |
| `radio_sed` | `target_name`:string, `ra`:number, `dec`:number, `radius_arcsec`:number | Compile a compact-source radio continuum SED from TGSS, GLEAM, SUMSS, NVSS, and FIRST catalog fluxes; fit the spectral index alpha (S~nu^alpha), plot it, and return flags for resolution/epoch caveats. |
| `render_fits_image` | `url`:string\*, `title`:string, `colormap`:string, `stretch`:string | Download a FITS file from an archive URL and render it as a publication-quality image displayed inline in the chat. Use this when the user asks to SEE or VISUALIZE data. The URL should come from a prior archive search (access_url or datalink URL). |
| `sparcl_plot_spectrum` | `sparcl_id`:string\*, `mark_lines`:boolean, `smooth`:integer | Plot an actual SparCL optical spectrum by sparcl_id with optional model overlay and redshifted line markers. |
| `velocity_frame_distance` | `target_name`:string, `ra`:number, `dec`:number, `v_helio_kms`:number, `z`:number | Convert a heliocentric velocity or redshift to GSR, Local Group, and CMB frames and give Hubble-flow distances (Planck18 H0) -- use for nearby-galaxy distances and flow corrections. |
| `ztf_light_curve` | `oid`:string\* | Plot an ALeRCE/ZTF light curve for an alert object oid, including detections and non-detection limits. |
| `ztf_stamps` | `oid`:string\*, `candid`:string | Render ALeRCE/ZTF science, template, and difference stamp PNG panels for an alert object oid and optional candid. |

## `archive` (27)

| Tool | Parameters | Description |
|---|---|---|
| `find_alma_line_coverage` | `target_name`:string\*, `species`:string\*, `transition`:string\*, `redshift`:number, `tolerance_mhz`:number, `velocity_width_kms`:number | Resolve one named spectral transition with Splatalogue and return only ALMA projects whose exact spectral windows cover its observed frequency. |
| `gaia_distance` | `target_name`:string, `ra`:number, `dec`:number, `radius_arcsec`:number, `max_rows`:integer | Bailer-Jones (2021) geometric/photogeometric distances for Gaia DR3 sources near a position -- the correct way to turn parallax into distance for stars. |
| `galactic_extinction` | `target_name`:string, `ra`:number, `dec`:number, `bands`:array | Galactic dust reddening E(B-V) (SFD98 + Schlafly-Finkbeiner 2011) and per-band extinction A_lambda at a sky position -- use before any photometric correction, color, or distance-modulus work. |
| `hips_cutout` | `target_name`:string, `ra`:number, `dec`:number, `survey`:string, `fov_deg`:number, `width`:integer | Fetch a live CDS hips2fits PNG cutout for 'show me X', 'what does X look like', or multiwavelength postage-stamp requests. Supports aliases optical/dss/dss2, sdss, 2mass/nir, wise/mir, galex/uv, rosat/xray, fermi/gamma, vlass/radio, or raw HiPS IDs. Use this for broad survey imagery; hips2fits exposes roughly 1000 HiPS surveys. |
| `hips_multiband_panel` | `target_name`:string, `ra`:number, `dec`:number, `surveys`:array, `fov_deg`:number | Render a multi-panel CDS hips2fits survey view for appearance or multiwavelength postage-stamp requests. Defaults to optical, 2MASS, and WISE; failed panels are labeled. |
| `inspect_fits_header` | `access_url`:string\* | Read key metadata from a remote FITS file header WITHOUT downloading the full file. Returns beam size (BMAJ/BMIN in arcsec), RMS noise/sensitivity, rest frequency, target name, pixel scale, and image dimensions. Use after list_alma_files to inspect the data quality of each product. |
| `list_alma_files` | `mous_uid`:string\*, `filename_pattern`:string | List all deliverable files (images, cubes, continuum maps) for a given ALMA MOUS UID via the DataLink protocol. Returns filenames, sizes, and direct access URLs. Use after a search to discover which data products (e.g. *pbcor.fits) are available for download or remote FITS header inspection. |
| `match_cross_archive_sources` | `catalog_name`:string, `sources`:array, `archives`:array, `radius_arcsec`:number, `max_sources`:integer, `max_alma_rows`:integer, `max_mast_results_per_source`:integer, `require_all_archives`:boolean | Cross-match a source catalog against ALMA and MAST/JWST observations. Use this for source-list location questions such as Perseus protostars observed with ALMA and JWST, or pass explicit source coordinates for any catalog. |
| `match_perseus_protostars_alma_jwst` | `radius_arcsec`:number, `max_sources`:integer, `max_alma_rows`:integer, `max_mast_results_per_source`:integer | Cross-match a built-in Perseus protostar source list against ALMA and MAST/JWST observations. Use this for questions like 'Show locations of protostars in Perseus observed with ALMA and JWST'. Returns sources with both ALMA and JWST matches, counts, project/program IDs, and sky coordinates. |
| `moving_object_check` | `target_name`:string, `ra`:number, `dec`:number, `radius_deg`:number, `epoch`:string | List known asteroids/comets inside a field at a given epoch (IMCCE SkyBoT) — use to check whether a transient or odd detection is a known moving object. |
| `ned_distance` | `target_name`:string\* | NED redshift-independent distance measurements (Cepheids, TRGB, SNIa, ...) for a named galaxy, with median summary. |
| `pulsar_lookup` | `name`:string\* | Look up a pulsar by J/B name in the ATNF catalogue and return its full timing/derived parameters. |
| `query_alma_science_archive` | `query_type`:string\*, `cycle`:integer, `target`:string, `band`:integer, `max_resolution_arcsec`:number, `arrays`:array, `lines`:array, `topic_filter`:string, `redshift_min`:number, `redshift_max`:number, `rest_species`:string, `science_category`:string, `require_same_project`:boolean, `include_adql`:boolean, `max_results`:integer | Run deterministic ALMA Science Archive query templates for hard archive-science questions. Use this instead of raw ADQL for: Cycle N project counts, Sun/solar projects, projects using 12m+7m+total-power arrays, high-resolution Band N continuum candidates for a target, projects covering a required molecular line set such as 12CO/13CO/C18O in the same project, and bandwidth-switching calibration diagnostics. |
| `search_pulsars` | `target_name`:string, `ra`:number, `dec`:number, `radius_deg`:number, `max_rows`:integer | Search the ATNF pulsar catalogue around a sky position; returns period, DM, 1400 MHz flux, distance, binarity, associations. |
| `search_space_lightcurves` | `target`:string\*, `mission`:string, `max_rows`:integer | List available TESS/Kepler/K2 light curves for a target; returns a table whose index can be used with plot_space_lightcurve or period_search. |
| `search_ztf_alerts` | `target_name`:string, `ra`:number, `dec`:number, `radius_arcsec`:number, `max_rows`:integer | Search ALeRCE/ZTF alert objects near a target or sky position for transients and variability; returns a UI table of object IDs and detection metadata. |
| `solar_system_ephemeris` | `target`:string\*, `start`:string\*, `stop`:string\*, `step`:string | JPL Horizons ephemeris for a planet, asteroid, or comet over a date range: RA/Dec, observer/heliocentric distances, magnitude, and elongation. |
| `sparcl_find_spectra` | `target_name`:string, `ra`:number, `dec`:number, `radius_arcsec`:number, `data_release`:array, `limit`:integer | Search NOIRLab SparCL for actual DESI/SDSS optical spectra near a target or position, not just redshift catalog values. |
| `survey_coverage` | `target_name`:string, `ra`:number, `dec`:number, `radius_deg`:number, `dataproduct_type`:string, `keyword`:string, `regime`:string, `max_rows`:integer | List surveys/datasets whose sky coverage (MOC) includes a position or region; use first for data-existence/coverage questions before broad archive searches, with optional dataproduct_type, regime, or keyword filters. |
| `survey_covers_position` | `survey_keyword`:string\*, `target_name`:string, `ra`:number, `dec`:number | Check whether a named survey (e.g. VLASS, SDSS, GLEAM) covers a given position; returns covered true/false and matching dataset IDs. |
| `triage_alma_data_products` | `identifier_or_target`:string\*, `band`:string, `max_projects`:integer, `max_mous`:integer, `max_products`:integer, `max_header_checks`:integer | Discover and triage ALMA deliverable data products. Use this when the user asks to fetch, list, inspect, or triage ALMA FITS/data products. Exact project/proposal codes, MOUS UIDs, ASDM UIDs, and dataset IDs are routed directly. Generic target names are treated as ambiguous: the tool returns a project-code picker table and asks the user to choose before product triage. If a project picker was just shown, row-number replies such as '#4' or 'use number 4' are resolved to that project. It lists DataLink products and reads remote FITS headers only; it does not download large science files. |
| `vlass_cutout` | `target_name`:string, `ra`:number, `dec`:number, `fov_deg`:number | Fetch a VLASS 3 GHz radio-continuum cutout via hips2fits. Use for radio appearance; VLASS covers Dec > -40 deg only. |
| `vo_adql_query` | `access_url`:string\*, `adql`:string\*, `max_rows`:integer | Run a guarded SELECT-only ADQL query against any TAP service URL. On ADQL errors the server's message is returned - read it and fix the query. |
| `vo_cone_search` | `access_url`:string\*, `target_name`:string, `ra`:number, `dec`:number, `radius_deg`:number, `max_rows`:integer | Cone search any VO simple-cone-search service by position. |
| `vo_describe_table` | `access_url`:string\*, `table_name`:string\* | Column schema (names, datatypes, units, UCDs) of a table on any TAP service — call before writing ADQL. |
| `vo_find_services` | `keywords`:string\*, `service_type`:string, `waveband`:string, `max_rows`:integer | Discover Virtual Observatory services (TAP/SIA/SSA/cone) by keyword and waveband — finds archives Quasar has no built-in client for. Follow with vo_list_tables / vo_adql_query on the access_url. |
| `vo_list_tables` | `access_url`:string\*, `keyword`:string, `max_tables`:integer | List (and keyword-filter) the tables of any TAP service found via vo_find_services. |

## `datalab` (26)

| Tool | Parameters | Description |
|---|---|---|
| `datalab_catalog_scatter` | `result_id`:string\*, `x_expr`:string\*, `y_expr`:string\*, `color_by`:string, `invert_y`:boolean, `invert_x`:boolean, `title`:string, `x_label`:string, `y_label`:string, `overlay_locus`:string | Render a Data Lab result_id as a CMD/CCD/HR-style scatter plot using safe column expressions. x_expr/y_expr combine columns of the stored result with + - * / ** %, parentheses, numeric literals, and functions log10/log/sqrt/abs/exp/power (e.g. absolute magnitude: 'phot_g_mean_mag + 5*log10(parallax/100)' with parallax in mas). Only columns present in the stored result can be referenced — derived columns like M_G do NOT pre-exist; compute them inline here or alias them in the SQL SELECT first. |
| `datalab_color_color_diagram` | `catalog`:string\*, `table`:string, `radius_deg`:number, `ra`:number, `dec`:number, `target_name`:string, `x_bands`:array, `y_bands`:array, `split_col`:string, `split_threshold`:number, `limit`:integer, `point_sources`:boolean, `value_cuts`:array | ONE-SHOT color-color diagram (e.g. g-r vs r-i) for a catalog cone. Queries + plots in a single call; auto-splits into stars vs galaxies (2 panels) using the catalog's morphology column (e.g. DES spread_model_r) unless split_col is given. Sentinel magnitudes (99.99) are excluded automatically. Use this for 'show me a color-color diagram'/'separate stars from galaxies' requests — do NOT chain separate query+plot tools. |
| `datalab_color_image` | `ra`:number, `dec`:number, `target_name`:string, `fov_deg`:number\*, `catalog`:string, `endpoint`:string, `bands`:array, `q`:number, `stretch`:number, `title`:string | Render a Data Lab color image from deepest SIA stack images, reprojected to a common WCS before Lupton RGB composition. Auto-selects RGB bands (red=i or z, green=r, blue=g) unless 'bands' is given. |
| `datalab_color_magnitude_diagram` | `catalog`:string\*, `table`:string, `radius_deg`:number, `ra`:number, `dec`:number, `target_name`:string, `blue_band`:string, `red_band`:string, `mag_band`:string, `limit`:integer, `point_sources`:boolean, `value_cuts`:array | ONE-SHOT color-magnitude diagram (CMD): mag_band vs (blue-red) color for a catalog cone. Queries + plots in a single call (magnitude axis inverted). Sentinel magnitudes (99.99) are excluded automatically; set point_sources=true when the user asks for stars/point sources. Use this for 'plot a CMD'/'g vs g-r' requests instead of chaining query+plot tools. |
| `datalab_cone_count` | `catalog`:string\*, `table`:string\*, `ra`:number\*, `dec`:number\*, `radius_deg`:number\* | Count rows in a Data Lab catalog cone using a governed q3c_radial_query builder. |
| `datalab_confirm_sky_area` | `ra_min`:number\*, `ra_max`:number\*, `dec_min`:number\*, `dec_max`:number\*, `tile_radius_deg`:number | Estimate the sky area and tile count for a tiled search before fanning out (guardrail: confirm wide scans with the user first). |
| `datalab_cutout_grid` | `peaks`:array\*, `fov_deg`:number\*, `band`:string, `catalog`:string, `endpoint`:string, `title`:string | Render a multi-panel Data Lab SIA cutout grid for peak coordinates; panels without coverage are labeled instead of failing the grid. |
| `datalab_density_aggregate` | `catalog`:string\*, `table`:string\*, `mode`:string, `step_deg`:number, `healpix_column`:string, `ra`:number, `dec`:number, `radius_deg`:number, `all_sky`:boolean, `color_cut`:object, `value_cuts`:array, `morphology`:object, `limit`:integer | Aggregate Data Lab source density by RA/Dec grid or registered HEALPix column over a cone region; returns a stable result_id. Requires a cone (ra/dec/radius_deg) unless all_sky=true is set explicitly. Wide cones that exceed the 60s sync window are automatically tiled into sub-cones and merged — do NOT hand-tile the region yourself; call once with the full cone. |
| `datalab_density_vetting` | `catalog`:string\*, `table`:string\*, `radius_deg`:number\*, `ra`:number, `dec`:number, `target_name`:string, `step_deg`:number, `color_cut`:object, `value_cuts`:array, `morphology`:object, `top_n`:integer, `fov_deg`:number, `band`:string | P12: find the densest catalog cells within a cone (with optional color/magnitude/morphology cuts) and pull a SIA cutout grid of the top-N densest locations to eyeball. |
| `datalab_describe_table` | `catalog`:string\*, `table`:string\* | Describe a registered Data Lab catalog table, columns, region strategy, morphology hints, and citation. |
| `datalab_export_notebook` | `title`:string, `sql`:string, `catalog`:string, `table`:string, `sia_ra`:number, `sia_dec`:number, `sia_fov_deg`:number, `sia_endpoint`:string, `svo_filters`:array | Export a reproducible Jupyter notebook for a Data Lab analysis: the governed TAP SQL (qc.query(sql=...)), an optional SIA cutout recipe, an optional SVO filter-wavelength lookup, and a data-citation cell. |
| `datalab_get_result` | `result_id`:string\*, `max_rows`:integer | Fetch the rows of a stored Data Lab result_id (from a prior datalab_* tool), capped at max_rows (<=5000). |
| `datalab_image_cutout` | `ra`:number, `dec`:number, `target_name`:string, `fov_deg`:number\*, `band`:string, `catalog`:string, `endpoint`:string, `title`:string | Render a single-band NOIRLab Astro Data Lab SIA cutout at RA/Dec or a resolvable target name. |
| `datalab_job_cancel` | `job_id`:string\* | Cancel a running Data Lab background job. |
| `datalab_job_results` | `job_id`:string\* | Fetch the result of a Data Lab background job (ranked candidates for a tiled search). |
| `datalab_job_status` | `job_id`:string\* | Poll the status of a Data Lab background job (e.g. a tiled search). |
| `datalab_list_catalogs` | — | List supported NOIRLab Astro Data Lab P0 catalogs and registered tables. |
| `datalab_lss_wedge` | `result_id`:string\*, `ra_col`:string, `dec_col`:string, `z_col`:string, `class_col`:string, `pie_slice`:boolean, `title`:string | Render a stored spectroscopic Data Lab result_id as a comoving large-scale-structure wedge or 3D scatter plot. |
| `datalab_period_fold` | `result_id`:string\*, `time_col`:string, `mag_col`:string, `error_col`:string, `band`:string, `band_col`:string, `min_frequency`:number, `max_frequency`:number, `title`:string | Run Lomb-Scargle period search on a stored Data Lab light curve and render the folded light curve. Multi-band light curves (NSC/DES/SMASH interleave g/r/i/z epochs in one table) should be folded ONE band at a time — pass band (e.g. 'g') to restrict to a single filter; mixing bands smears the phased curve. |
| `datalab_q3c_crossmatch` | `small_catalog`:string, `small_table`:string, `big_catalog`:string, `big_table`:string, `ra`:number\*, `dec`:number\*, `radius_deg`:number\*, `match_radius_arcsec`:number, `small_columns`:array, `big_columns`:array, `small_limit`:integer, `limit`:integer | Planner-safe Data Lab q3c crossmatch: materializes the small Gaia-like side first and joins the large indexed catalog second. |
| `datalab_sed_plot` | `result_id`:string\*, `row_index`:integer, `filter_columns`:object, `title`:string | Render an LS DR9-style SED from a stored Data Lab result_id using SVO FPS wavelengths. |
| `datalab_select_catalog_rows` | `catalog`:string\*, `table`:string\*, `ra`:number\*, `dec`:number\*, `radius_deg`:number\*, `columns`:array, `limit`:integer, `value_cuts`:array, `color_cut`:object, `morphology`:object | Select capped rows from a Data Lab catalog cone using governed structured SQL; returns result_id, not the full table. Apply selection cuts server-side via value_cuts/color_cut/morphology so the row budget is spent on rows you want. |
| `datalab_sky_density_map` | `result_id`:string\*, `mode`:string, `ra_col`:string, `dec_col`:string, `count_col`:string, `bins`:integer, `healpix_col`:string, `nside`:integer, `order`:string, `matched_filter`:boolean, `sigma_small`:number, `sigma_large`:number, `peak_threshold`:number, `max_peaks`:integer, `log_scale`:boolean, `title`:string | Render a Data Lab result_id as a RA/Dec density map or sparse HEALPix density map, with optional matched-filter peak detection. The colorbar is LOG-scaled by default (log_scale=true) — leave it on for 'log counts'/'log source count' requests; set log_scale=false only when the user explicitly wants a linear count scale. |
| `datalab_sql_query` | `sql`:string\*, `expert_ack`:boolean\*, `reason`:string\* | EXPERT/DEBUG ONLY: run governed raw native SQL against Data Lab. Requires expert_ack=true and a reason; q3c_join remains blocked outside the structured crossmatch builder. Returns result_id only, not the full table. |
| `datalab_tiled_search` | `catalog`:string\*, `table`:string\*, `ra_min`:number\*, `ra_max`:number\*, `dec_min`:number\*, `dec_max`:number\*, `tile_radius_deg`:number, `step_deg`:number, `color_cut`:object, `value_cuts`:array, `morphology`:object, `peak_threshold`:number, `max_tiles`:integer, `candidate_budget`:integer, `confirm`:boolean | P15: tiled region-bounded overdensity search over a footprint. Runs a server-side density aggregate per q3c cone tile, finds matched-filter peaks, and ranks candidates. Executes as a background job; for a large area it returns needs_confirmation first — re-call with confirm=true after confirming the sky area with the user. |
| `svo_filter_wavelength` | `filter_id`:string, `filter_ids`:array | Look up effective and pivot wavelengths for SVO FPS filter IDs, including LS DR9 shorthand g/r/z/w1/w2. |

## `general` (44)

| Tool | Parameters | Description |
|---|---|---|
| `advanced_search` | `query`:string\* | Execute a custom ADQL/TAP query directly on the ALMA Science Archive (ivoa.obscore table). ALMA ONLY — NOT for NOIRLab Data Lab catalogs (gaia_dr3/des_dr1/desi_dr1/nsc_dr2/smash/...): use datalab_sql_query for those. IMPORTANT: The obscore table has NO 'redshift' column. Use frequency/bandwidth containment instead. To find observations covering a specific frequency nu_ghz: WHERE (frequency - 0.5*bandwidth/1e9) < {nu_ghz} AND (frequency + 0.5*bandwidth/1e9) > {nu_ghz} Key columns: target_name, s_ra, s_dec, frequency (GHz), bandwidth (Hz), scientific_category, science_keyword, proposal_id, member_ous_uid, t_exptime, s_resolution, band_list. Add OFFSET 0 ROWS FETCH NEXT 500 ROWS ONLY to limit results. |
| `analyze_uv_coverage` | `ms_path`:string\* | Analyze UV coverage for an observation |
| `check_co_lines` | `z`:number | Check for CO, 13CO, and C18O lines in the LAST search results. |
| `check_line_coverage` | `line_freq_ghz`:number\*, `z`:number, `line_name`:string | Check if specific lines are covered in the LAST search results. |
| `click_element` | `selector`:string\*, `wait_after_ms`:integer | Click a button, link, or other element on the current browser page by CSS selector. |
| `cross_match_source` | `target_name`:string\*, `archives`:array | Query multiple astronomical archives (Simbad, NED, MAST, VizieR, Fermi 4FGL) in parallel for a source. Returns a unified multi-wavelength summary. |
| `download_alma_data` | `dry_run`:boolean | Download ALMA data (FITS) for current results |
| `download_data` | `obs_id`:string\* | Legacy download tool (Use download_alma_data instead) |
| `download_mast_data` | `product_type`:string, `extension`:string, `max_files`:integer | Download FITS files from the MAST archive (JWST/HST data). Call this AFTER search_mast to download actual science data files. Downloads to ~/quasar_data/mast/ by default. Has a safety limit of 10 files. |
| `filter_results` | `column`:string\*, `operator`:string\*, `value`:number\* | Apply numeric filters to the LAST ALMA/archive search results table (e.g. 'resolution < 0.05 arcsec', 'sensitivity > 10 mJy'). This does NOT see Data Lab catalog results — for those, put the cut in the query itself (datalab_select_catalog_rows value_cuts, datalab_sql_query WHERE) or use the one-shot diagram tools' point_sources/morphology options. |
| `generate_casa_calibration_script` | `target`:string\*, `vis`:string\*, `flux_cal`:string\*, `phase_cal`:string\*, `refant`:string\* | Generate a CASA manual calibration script for ALMA/VLA data reduction. Returns ready-to-run Python code for bandpass, gain, and flux calibration. |
| `generate_casa_imaging_script` | `target`:string\*, `vis`:string\*, `band`:string, `cell`:string, `imsize`:integer, `weighting`:string, `robust`:number, `threshold`:string, `specmode`:string | Generate a complete CASA tclean imaging script for ALMA/VLA data. Returns ready-to-run Python code for radio interferometry imaging. |
| `get_latest_gw_events` | `n`:integer, `event_type`:string | Get the latest gravitational wave events from the GWOSC (Gravitational Wave Open Science Center) catalog. Use for multi-messenger astronomy queries. |
| `get_mast_products` | `product_type`:string, `extension`:string | Get file-level product list for the LAST MAST search results. Shows individual data files available for download (filenames, sizes, URLs). Call this AFTER a search_mast or search_mast_by_criteria call. |
| `get_observation_details` | `obs_id`:string\* | Get detailed information about a specific observation |
| `get_sky_image` | `target_name`:string, `survey`:string, `radius_arcmin`:number, `ra`:number, `dec`:number | Fetch a sky survey cutout image for a target. Returns a FITS file and PNG preview from surveys like DSS2 (optical), 2MASS (near-IR), SDSS (optical), WISE (mid-IR), NVSS/FIRST (radio). Use this when users ask for 'an image of', 'show me', 'DSS image', or 'what does X look like'. |
| `identify_spectral_line` | `frequency_ghz`:number\*, `tolerance_ghz`:number, `top_n`:integer | Identify molecular spectral lines near a given rest frequency using the Splatalogue database. Essential for ALMA/VLA spectral line identification. |
| `navigate_to_url` | `url`:string\* | Navigate the browser to a specific full http(s) URL and return its page content. Use for interactive browser-only tasks on ALMA archive, NASA ADS, ESO portal, VizieR, arXiv pages, etc. Do NOT use this for keyword searches and do NOT call it after web_search unless the user explicitly asked to open/navigate to a specific URL. |
| `plot_alma_results` | `plot_type`:string, `x_column`:string, `y_column`:string, `color_by`:string, `title`:string, `dark_mode`:boolean | Generate a plot from the last ALMA search results. Two modes: 1. Quick overview: pass plot_type='sky', 'frequency', or 'overview' for pre-built plots. 2. Publication-quality scatter: pass x_column and y_column for a custom ApJ/MNRAS-style scatter plot (300 DPI, colorblind-safe). If plot_type is given, x_column/y_column are ignored. Use after any search. |
| `plot_sky_map` | `ra_col`:string, `dec_col`:string, `color_by`:string, `title`:string | Generate a publication-quality RA/Dec sky distribution map from the last ALMA search results. |
| `plot_spectrum` | `frequencies`:array\*, `fluxes`:array\*, `title`:string, `errors`:array | Generate a publication-quality 1D spectral line profile plot with optional error bars and molecular line ID labels. |
| `read_page` | — | Read the text content of the currently open browser page. Call after navigate_to_url to get the full page content. |
| `resolve_target` | `target_name`:string\* | Resolve a target name to RA/Dec coordinates using SIMBAD. Use this if search_by_target returns empty for a valid target name. |
| `search_alma_co_in_redshift_range` | `z_min`:number\*, `z_max`:number\*, `science_category`:string, `max_results`:integer | Search the ALMA archive for observations that cover CO emission lines for galaxies at a given redshift range. Handles the CO rest-frequency → observed-frequency conversion and TAP frequency-containment query automatically. Use this for any query like 'galaxies at z=1-2 with CO coverage' or 'ALMA CO detections at high redshift'. CO transitions checked: J=1-0 (115.3 GHz), J=2-1 (230.5), J=3-2 (345.8), J=4-3 (461.0), J=5-4 (576.3), J=6-5 (691.5), J=7-6 (806.7). Returns: target_name, proposal_id, CO_transition, obs_frequency_ghz, bandwidth_ghz. |
| `search_alma_with_keywords` | `keywords`:object\* | Search ALMA archives using specific keywords (pi_name, project_code, etc.) |
| `search_by_frequency` | `min_freq_ghz`:number\*, `max_freq_ghz`:number\*, `facility`:string, `max_results`:integer | Search archives by frequency range. Defaults to ALMA; pass facility='VLA'/'VLBA'/'GBT' for the NRAO archive. |
| `search_by_position` | `ra`:number\*, `dec`:number\*, `radius`:number, `facility`:string, `band`:string, `scan_intent`:string, `max_results`:integer | Search NRAO archives by sky position (cone search) |
| `search_by_target` | `target_name`:string\*, `facility`:string, `band`:string, `max_resolution`:number, `min_resolution`:number, `min_freq_ghz`:number, `max_freq_ghz`:number, `min_exp_s`:number, `scan_intent`:string, `public_only`:boolean | Search the ALMA archive (default) by target name. Supports multiple targets separated by 'and' or comma (e.g. 'M87 and Sz65' or 'M87, NGC 1068'). Pass facility='VLA', 'VLBA', or 'GBT' to search the NRAO archive instead — ONLY when the user explicitly asks for those telescopes. CRITICAL: ONLY pass optional filter parameters (band, resolution, frequency, scan_intent) if the user EXPLICITLY mentions them. Do NOT invent default values. If the user just says 'Find ALMA data of M87', pass ONLY target_name='M87' with NO other parameters — this returns ALL observations across all bands. Only pass band=6 if the user says 'Band 6'. Only pass max_resolution if the user specifies a resolution constraint. Omitting a filter means 'no filter'. |
| `search_cadc_archive` | `target_name`:string, `ra`:number, `dec`:number, `radius`:number, `collection`:string, `max_results`:integer | Search the Canadian Astronomy Data Centre (CADC) for multi-wavelength observations from JWST, HST, JCMT, CFHT, Gemini, and other telescopes. Uses the IVOA ObsCore TAP service. Complements ALMA searches with optical/infrared/submm data. |
| `search_catalog` | `objects`:array\* | Search for a catalog of objects (Name, RA, Dec) |
| `search_eso_archive` | `target_name`:string, `instrument`:string, `ra`:number, `dec`:number, `radius_arcmin`:number | Search the ESO Science Archive for VLT instrument observations. Supports instruments: MUSE, KMOS, X-Shooter, FORS2, HAWK-I, UVES, SPHERE, GRAVITY, ESPRESSO, and more. Uses TAP/ADQL queries against the ESO ObsCore table. |
| `search_gwtc_catalog` | `mass_min_solar`:number, `mass_max_solar`:number, `distance_max_mpc`:number, `event_type`:string | Search the GWTC gravitational wave transient catalog with mass, distance, and type filters. |
| `search_irsa` | `target_name`:string, `catalog`:string, `radius_arcsec`:number, `ra`:number, `dec`:number | Search the IRSA (Infrared Science Archive) catalog services for infrared source catalogs. Catalogs include AllWISE, 2MASS Point Source, 2MASS Extended Source, and Spitzer SEIP. |
| `search_lines_by_molecule` | `molecule_name`:string\*, `freq_min_ghz`:number, `freq_max_ghz`:number, `top_n`:integer, `transition`:string, `energy_min`:number, `energy_max`:number, `energy_type`:string, `intensity_lower_limit`:number, `intensity_type`:string, `line_lists`:array, `only_astronomically_observed`:boolean, `only_nrao_recommended`:boolean | Search Splatalogue for spectral-line transitions of a molecule, with optional frequency, energy, intensity, transition, and catalog filters. |
| `search_mast` | `target_name`:string, `mission`:string, `instrument`:string, `radius`:string, `ra`:number, `dec`:number | Search the MAST archive for observations from JWST, HST, TESS, Kepler, and other space telescopes. Use this for any JWST or HST data queries. Returns observation metadata including target, instrument, filters, exposure time, and data product type. |
| `search_mast_by_criteria` | `mission`:string, `instrument`:string, `proposal_id`:string, `filters`:string, `target_name`:string, `dataproduct_type`:string, `start_date`:string, `end_date`:string | Advanced MAST search with rich filtering: program ID, date range, filter name, data product type, etc. Use this when users ask for specific JWST/HST programs, particular filters (F200W, F444W), or time-constrained searches. |
| `search_spectral_lines` | `freq_min_ghz`:number\*, `freq_max_ghz`:number\*, `molecule_name`:string, `transition`:string, `energy_min`:number, `energy_max`:number, `energy_type`:string, `intensity_lower_limit`:number, `intensity_type`:string, `version`:string, `exclude`:array, `line_lists`:array, `only_astronomically_observed`:boolean, `only_nrao_recommended`:boolean, `top_n`:integer | Run an advanced Splatalogue frequency-range query. Use this for filtered line surveys, line-confusion checks, and catalog comparisons. |
| `summarize_gcn_circular` | `circular_number`:integer\* | Fetch and parse a NASA GCN (Gamma-ray Coordinates Network) circular by number. Extracts key parameters: event name, trigger time, coordinates, and summary. |
| `web_crawl_site` | `url`:string\*, `instructions`:string, `chunks_per_source`:integer, `max_depth`:integer, `max_breadth`:integer, `limit`:integer, `extract_depth`:string, `content_format`:string, `include_images`:boolean, `select_paths`:array, `exclude_paths`:array, `select_domains`:array, `exclude_domains`:array, `allow_external`:boolean | Crawl a bounded website section with Tavily Crawl and extract content from discovered pages. Use for documentation sections or site areas where multiple pages are needed. Keep limit small unless the user asks for broad coverage. |
| `web_extract_url` | `urls`:string\*, `query`:string, `chunks_per_source`:integer, `extract_depth`:string, `include_images`:boolean, `content_format`:string | Extract clean markdown/text from one or more specific URLs using Tavily Extract. Use when the user gives URLs and asks to read, summarize, quote, or pull page content. Only pass full http(s) URLs. Never pass search terms or keyword queries here; use web_search for those. Do not call this immediately after web_search unless the final answer truly needs full-page text from a specific result URL. For JavaScript-heavy pages, set extract_depth='advanced'. |
| `web_map_site` | `url`:string\*, `instructions`:string, `max_depth`:integer, `max_breadth`:integer, `limit`:integer, `select_paths`:array, `exclude_paths`:array, `select_domains`:array, `exclude_domains`:array, `allow_external`:boolean | Discover URLs on a website using Tavily Map without extracting page content. Use before crawling a large site, or when the user asks for site structure or to find the right page. |
| `web_research` | `research_input`:string\*, `model`:string, `citation_format`:string, `wait_for_completion`:boolean, `timeout_seconds`:integer | Run Tavily Research for comprehensive multi-source web research with citations. Use for deep web reports, market/landscape comparisons, or current-topic investigations. Do not use for astronomy paper searches; use search_papers for publications. |
| `web_research_status` | `request_id`:string\* | Get the status or completed content for a Tavily Research request_id. |
| `web_search` | `query`:string\*, `max_results`:integer, `search_depth`:string | Search the web for real-time information: astronomy news, telescope schedules, observatory announcements, instrument specs, call-for-proposals, or operational status updates. NEVER use this for finding papers or publications — use search_papers (NASA ADS) instead. Uses Brave, Tavily, and Exa through Quasar's web search router for grounded, source-cited results. Use the user's query as-is — do NOT add years or dates unless the user explicitly mentioned them. For keyword queries, use this tool directly and answer from its returned sources. Do NOT call navigate_to_url after web_search unless the user explicitly asks to open a specific result URL or the search result is insufficient and you need one full page from a known http(s) URL. Examples: 'ALMA proprietary period policy', 'JWST cycle 4 call for proposals', 'VLA sensitivity at 1.4 GHz'. |

## `literature` (16)

| Tool | Parameters | Description |
|---|---|---|
| `add_to_ads_library` | `library_id`:string\*, `bibcodes`:any\* | Add papers to an existing personal ADS library by their bibcodes. |
| `create_ads_library` | `name`:string\*, `description`:string, `public`:boolean | Create a new personal ADS paper library to organize papers by topic or project. |
| `evaluate_consensus` | `question`:string\*, `max_papers`:integer | Evaluate the scientific consensus on a research question by searching NASA ADS for the most-cited papers on the topic, reading all their abstracts, and producing a structured analysis. The output includes: overall consensus level (Strong Agreement / Divided / etc.), which specific papers agree vs disagree, WHY they disagree (different methods, data, assumptions), key evidence from each side with proper citations, how the consensus has evolved over time, and what open questions remain. Use this when the user asks questions like: 'Do scientists agree on X?', 'What does the field think about X?', 'Is there consensus on X?', 'What's the current understanding of X?', or any question where a literature-wide summary would be more useful than individual paper results. |
| `export_bibtex` | `bibcodes`:any\* | Export properly formatted BibTeX citations for one or more papers given their ADS bibcodes. Use when the user asks to cite papers or needs BibTeX. |
| `extract_paper_details` | `identifier`:string\*, `query`:string\* | Download a scientific paper by its arXiv ID or ADS bibcode and extract specific details (e.g. beam size, flux density, telescope configuration) using an LLM QA pass over the full text. Use when the user asks specific questions about the contents of a published paper. |
| `get_ads_library_papers` | `library_id`:string\*, `max_results`:integer | Get papers stored in a specific personal ADS library by library ID. |
| `get_author_metrics` | `author`:string\* | Calculate scholarly impact metrics for an author: h-index, i10-index, total citations, refereed citations. |
| `get_author_papers` | `author`:string\*, `max_results`:integer | Find all papers published by a specific author. Use 'Last, First' format for best results. |
| `get_paper_abstract` | `bibcode`:string\* | Get the full abstract and detailed metadata (keywords, affiliations) for a specific paper by bibcode. |
| `get_paper_metrics` | `bibcode`:string\* | Get citation count, read statistics, and impact metrics for a specific paper by its ADS bibcode. |
| `get_research_trends` | `query`:string\*, `year_from`:integer, `year_to`:integer | Get a bibliometric trend showing papers-per-year for a given topic or search query. Returns total paper count and yearly breakdown. Use when the user asks 'How much research is being done on X?', 'Is interest in X growing?', 'Publication trends for FRBs'. Also returns the funding landscape — which funders (NSF, NASA, ESA, etc.) have funded research on the topic. |
| `list_ads_libraries` | — | List all personal ADS paper libraries for the authenticated ADS user. |
| `lookup_researcher` | `query`:string\*, `max_results`:integer | Look up a researcher/scientist by name or ORCID to get their full academic profile: current institution, h-index, i10-index, total publications, total citations, ORCID, Scopus ID, research topics, affiliation history, and publication trend over the last 10 years. Powered by OpenAlex (90M+ disambiguated authors). Use this when the user asks about a person, wants to know who someone is, or wants contact/institutional information about a researcher. Examples: 'Who is Andrea Isella?', 'Tell me about Crystal Brogan', 'Look up ORCID 0000-0001-2345-6789' |
| `reproduce_paper_methods` | `identifier`:string\* | Extract the methodology from a published paper (by arXiv ID or ADS bibcode) and construct a Python/CASA data reduction script that replicates its steps. Use when a user asks 'how did they reduce the data for this paper' or 'reproduce this paper'. |
| `search_papers` | `query`:string\*, `max_results`:integer, `sort`:string | Search the NASA ADS database for astronomical papers. Returns titles, authors, abstracts, citation counts, DOIs, and a direct link to each paper on NASA ADS. IMPORTANT: Pass the user's request as natural language — an internal AI query builder will automatically translate it into optimal ADS syntax using keyword searches, bibgroup filters, SIMBAD object linking, second-order discovery operators (trending, similar, useful), and more. Examples of what to pass as query: - 'recent papers on protoplanetary disks' - 'best ALMA papers on disk gaps' - 'papers about HL Tau' - 'what are people reading about FRBs right now' - 'foundational papers on planet formation' - 'review articles on AGN feedback' - 'papers by Sean Andrews on disk surveys' Do NOT try to construct ADS field syntax yourself — just pass the natural language query. |
| `search_papers_by_observation_id` | `identifier`:string\*, `facility`:string, `max_results`:integer | Find NASA ADS papers explicitly connected to a specific archive identifier. Use this instead of generic search_papers when the user provides an ALMA project/proposal code (e.g. 2019.1.00123.S), MOUS/member_ous_uid (uid://...), ASDM UID, or archive dataset ID. The lookup uses exact identifier searches and returns provenance metadata for the graph. |

## `mmu_hats` (3)

| Tool | Parameters | Description |
|---|---|---|
| `crossmatch_mmu_hats_catalogs` | `left_catalog_key`:string\*, `right_catalog_key`:string\*, `ra`:number\*, `dec`:number\*, `radius_arcsec`:number, `match_radius_arcsec`:number, `columns_left`:array, `columns_right`:array, `max_rows`:integer | Crossmatch two Multimodal Universe HATS catalogs (e.g. gaia x desi_edr_sv3) within a bounded cone region using LSDB margin caches. Requires ra/dec/radius -- all-sky crossmatches are not allowed. If this fails, run two bounded search_mmu_hats_catalog cone searches instead. |
| `list_mmu_hats_catalogs` | — | List available Multimodal Universe HATS catalogs (Gaia, DESI, SDSS, TESS, Chandra) that Quasar can cone-search from Hugging Face via LSDB. These provide catalog/source properties, not archive observations. |
| `search_mmu_hats_catalog` | `catalog_key`:string, `ra`:number, `dec`:number, `radius_arcsec`:number, `columns`:array, `max_rows`:integer, `target_name`:string | Cone-search a Multimodal Universe HATS catalog using LSDB/Hugging Face. Use this for catalog/source properties such as Gaia astrometry, DESI/SDSS redshifts, TESS source metadata, Chandra spectra metadata, or cross-survey source enrichment around a sky position. Do NOT use this for finding archive observations, project/proposal IDs, or FITS/data products -- use the archive tools for those. |
