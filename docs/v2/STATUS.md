# STATUS.md — Living progress tracker

**Update this after every meaningful change.** Newest entry on top. This is the single source of truth for "where are we."

## Current state

- **API monolith split — LANDED (this session, api-split track):** `ui-pro/api/main.py` (4,368 → ~250 lines) split into `api/routers/*` (17 route modules) + `api/sse.py` (chat SSE state machine) + `api/serializers/{data_card,cadc,fits}.py` (column maps / CADC XML / matplotlib) + `api/deps.py` (shared singletons/helpers) + `api/models.py` + `api/bootstrap.py`. **S4** (CORS explicit allowlist) + **S5** (auth on `conductor/traces*` + `proposals/review`) fixed in the same pass. Exact 68-route parity, boot-verified (login + chat SSE), 5-agent adversarial review → 0 divergences. Composes cleanly with the concurrent S6 cookie-auth layered on the same routers. *(P1 gate "`main.py` < ~1,500 lines" ✅; the `agent.py` half is the other track's work.)*
- **Phase:** P1 shared core — **22 of ~25 Data Lab tools** migrated to capabilities and inline-retired (agent.py 12,714→**12,208**). **Request-scoped-state pattern established:** per-turn mutable state (the aggregate sync-timeout table set, the job poll-count dict) is owned by the agent, reset per turn in `stream_response_api`, and **injected into `CallContext.services`** (re-read per tool call, never cached) so capabilities mutate it without touching `self`. Every later family reuses this. Remaining (special handling): `density_vetting` (nested image attach), `tiled_search` (job start), `export_notebook` (custom card, must NOT clear `last_run_result`). Then retire `_datalab_error`/`_datalab_fit_rows`/`_datalab_coordinates`, then ALMA. *(Benchmark gate paused per user instruction; migrations verified by unit tests + boot.)*
- **Inline retirement (agent.py shrinks):** **15 Data Lab tools now capability-backed by default** with inline copies deleted — 7 SQL via `_datalab_tool_fn`, and 8 image tools via `_datalab_image_tool_fn` (5 result_id plots: `catalog_scatter`/`sky_density_map`/`period_fold`/`sed_plot`/`lss_wedge`; 3 SIA: `image_cutout`/`color_image`/`cutout_grid`). The image-card transport (`_datalab_attach_image_result`) moved to the adapter boundary via `_datalab_image_tool_fn` (captions passed through a private `_caption` key so nothing leaks to the LLM dict); capabilities stay transport-pure (client/store/**image-service**/**coordinate-resolver**/SVO injected via CallContext). `QUASAR_USE_CAPABILITY_DATALAB` flag removed. `core/agent.py`: **12,714 → 12,428** (−286 net despite all added P0/P1 code). Contract tests dispatch through the registry; real-`__init__` boot confirmed. SQL-tool runtime byte-identical to the flag-ON path already benchmarked at 65.6; plot/SIA/SED-SVO/band-substitution plumbing covered by new unit tests.
- **Handoff noted (from Track B):** `agent_pool`/`context_manager`/`session_memory`/`health_monitor` are wired in `core/agent.py` (~:354–363) and left for me (agent.py owner) to remove so Track B can delete the modules.
- **P1 delivered this session:** `capabilities/base.py` (ToolResult / CallContext / Provenance / Budget / ResultStore / Capability / BaseCapability / ServiceClient); extended `core/tools.py` registry (namespacing, validation, collision protection, intent-subsetting); `adapters/native/` (capability→Tool bridge); `capabilities/datalab.py` (7 SQL/catalog tools relocated verbatim, transport-pure); wired into `core/agent.py` behind `QUASAR_USE_CAPABILITY_DATALAB` (default OFF). Tests: `test_capabilities_base.py` (15✓) + `test_datalab_capability.py` (8✓).
- **Parity gate: ✅ PASS.** Flag-ON DataLabBench = **65.6 overall / 74.2 auto** vs **65.5 / 72.3** baseline (`Benchmark/datalabbench/results/p1_datalab_capability_gpt-4.1/PARITY.md`). Deltas within run-to-run variance; the migrated tools' tiers held.
- **Adversarial review (4-dim / 9-agent):** 3 distinct issues, all in `capabilities/datalab.py` — A) expert-ack gate loosened by pydantic `bool` coercion → **fixed** (strict `Any` identity); C) client fetched outside the try → **fixed** (moved in-try); B) `extra="ignore"` tolerates stray kwargs → **kept deliberately** (more robust, outcome-preserving). Regression test added.
- **P5 dead-code — partial pass this session:** deleted `agents/` (8 files; every `from agents` import was internal to the package — zero external importers), `core/workflow_store.py`, `core/model_council.py` (imported/instantiated nowhere), and `core/result_cache.py` (its only site was `core/conductor.py:306`, **not** agent.py — so removed the import at `conductor.py:36` + the instantiation; the hasattr-guarded reader at `ui-pro/api/main.py:4344` now yields `None`, so `main.py` was left untouched per constraint). **Proof nothing broke:** unit suite shows **no new failures** (same 8 pre-existing: `test_content_safety` web-images, `test_datalab_p1` ×3, `test_issue_report_service` ×4 — the collected count rose 462→475 from the concurrent main-line session's new registry-contract tests, not from this pass) **and** a deterministic `from api.main import app` boot succeeds with all four modules confirmed non-importable (`ModuleNotFoundError`). **Not deleted — wired in `core/agent.py`, which this pass must not edit (main-line owns these):** `agent_pool` (`agent.py:363`), `context_manager` (`agent.py:354`), `session_memory` (`agent.py:358`), `health_monitor` (`agent.py:360`, also injected into `model_router` at `:362` and read hasattr-guarded at `main.py:4355`). **Blocked:** `config/settings.py`+`config/__init__.py` are **NOT** "imported nowhere" as the analysis docs claim — the CI smoke test `.github/workflows/ci.yml:75-76` does `from config import settings` and reads `settings.api.openai_model`; that CI line must be dropped before/with the deletion.
- **Security (S3 + S6) hardening landed this session:** S3 **fully fixed** (no hard-coded admin; env allowlist + DB `role` column). S6 **password compare** (`hmac.compare_digest`) and **login rate limiting** fixed. S6 **httpOnly cookie: backend WIRED + verified** — the auth code had been split out of `main.py` into `api/routers/auth.py` + `api/deps.py` (Track B's P1 split) and **S4 CORS was already done there** (exact origins + `allow_credentials` + localhost regex), so cookie wiring went into the routers: `set_auth_cookie` on login/register/google, cookie fallback in `get_current_user`/`_resolve_optional_user`, a `POST /api/auth/logout`, and cookie-token threading through the chat/optional paths (`sse.py`/`chat.py`/`analytics.py`). **Bearer still works** (backward-compatible). **Frontend flip: DONE + browser-verified** (Fable 5 session, `@cx-build gpt-5.6-terra ultra`; Codex hit its usage limit after the consult+plan-gate, Claude finished solo per the limit-fallback rule). `auth-store.ts` no longer persists the JWT (persist `user` only; a `migrate` strips any legacy token; server-authoritative init via `GET /api/auth/me` with an auth-generation guard); all **54 backend fetches across 7 files** carry `credentials:'include'`; the ~20 `!token` guards (spectral-lines ×10, workbench ×5, sidebar, chat) migrated to `isAuthenticated`; `logout()` calls `POST /api/auth/logout`. Backend also: **HTTP 429 + `Retry-After`** for throttled logins (per-username AND a new per-IP limiter, `QUASAR_TRUST_XFF`-gated), and `expose_headers=["Retry-After"]` on CORS. Real-browser proof: login `1@1/1` → reload stays authed via the httpOnly cookie (no flash, `document.cookie` has no token) → spectral-lines/conversations load cookie-only → logout clears → 429 fires with a readable `Retry-After`. `tsc --noEmit` 0 errors; `tests/unit/test_s3_s6_security.py` 34✓ (58✓ with auth/quota/P0). Terra's consult caught the bootstrap race, `/me` field-loss, and XFF-spoofing before the build; the browser pass caught the CORS `expose_headers` gap that TestClient structurally can't.
- **(prior) Phase:** P0 stabilization — code fixes landed; baseline captured through the harness.
- **Code written toward V2:** P0 safety/correctness fixes (C1, C2, C3, C5, C6, C7, C9, C11, S1, S2) + offline regression suite `tests/unit/test_p0_stabilization.py` (15 tests, all green). No capabilities layer yet.
- **Benchmark baseline:** ✅ captured at git SHA `650c354`, model `gpt-4.1`, judge `gpt-4o` → `Benchmark/datalabbench/results/baseline_650c354_gpt-4.1/` (`BASELINE.md`). **Overall 65.5/100 (tier-weighted); 72.3/100 auto-only; 15/15 questions, 0 errors.** This is the P1 regression gate.
- **Verification:** all changed files compile; existing suites pass (ADS/RAG/facility-routing/datalab 72✓ — 3 pre-existing `test_datalab_p1` image-reproject failures confirmed failing on clean HEAD, unrelated). New P0 suite 19✓. **A 13-agent adversarial-review workflow over the diff found 6 confirmed defects (2 wrong-target C9, 1 C7 DECOMPOSE arity, 2 C3 masking sub-paths, +dup); all 6 fixed and covered by new regression tests.**
- **Next action:** P1 — `capabilities/base.py` (ToolResult/CallContext/Capability/ResultStore) + datalab reference migration (see `NEXT_SESSION_START_HERE.md` Step 3).

## Priority checklist (mirrors `IMPLEMENTATION_PRIORITIES.md`)

### P0 — Stabilize (prerequisite)
- [x] C1 fabrication: `services/analysis.py` mock UV → typed `casa_unavailable` error (agent wrapper now reflects `success=False`)
- [x] C2 fabrication: `integrations/ads_client.py` `_get_example_papers` deleted; no-key/error paths raise `ADSServiceError` → agent returns typed error
- [x] C3 fabrication: `services/search.py` failed searches now return an `.attrs["quasar_error"]`-tagged empty frame (backward compatible); the `search_by_target` handler (single-target **and** multi-target **and** per-target-band branches) plus `_search_by_position` now surface it as `success=False` instead of "No results found". *(Remaining minor handlers — frequency/keyword/source — get full typing with the ToolResult migration in P1.)* ⚠️ Discovered pre-existing bug: the positional-fallback gate at `agent.py:~5480` reads `resolved.get("ra")` but `_resolve_target` returns `ra_deg`/`dec_deg`, so that fallback is dead — logged for a follow-up (out of P0 scope).
- [x] S1 disable `exec()` user tools — gated behind `QUASAR_ENABLE_USER_TOOL_EXEC` (default OFF); `build_callable` raises, `_derive_schema` skips exec
- [x] S2 lock `/api/mcp-servers` command spawner — stdio gated behind `QUASAR_ENABLE_MCP_STDIO` (default OFF) at persistence (`save_server`) + agent spawn (`_load_mcp_servers`); http/SSE still allowed
- [x] S3 remove hard-coded `1@1` admin — `DEFAULT_ADMIN_EMAILS` now empty; admin via `ADMIN_EMAILS` env **or** `users.role='admin'` column (`is_admin_email` DB-aware, TTL-cached); local seed carries `role='admin'`
- [x] S6 password `==` → `hmac.compare_digest` (`services/auth.py`) + basic per-identity login rate limiting (`services/login_rate_limit.py`)
- [x] S6 JWT `localStorage` → httpOnly cookie — **DONE + browser-verified.** Backend: cookie set on login/register/google, read in `get_current_user`/`_resolve_optional_user`, `POST /api/auth/logout`, Bearer still works. Frontend: JWT no longer persisted (server-authoritative `/me` bootstrap + auth-generation guard), 54 fetches `credentials:'include'`, ~20 `!token` guards → `isAuthenticated`. Plus 429+`Retry-After` (per-user + per-IP) and CORS `expose_headers`.
- [x] Capture benchmark baseline (SHA `650c354`, gpt-4.1) → `Benchmark/datalabbench/results/baseline_650c354_gpt-4.1/`
- [x] (cheap wins) C5 RAG score-gate (gate on `_semantic_score`, not RRF `_score`), C6 rank_bm25 (added to requirements), C7 recovery bypass (route through `_execute_node` + enforce SLA), C9 DAGCache (target reconciliation w/ safety net), C11 dup `_filter_results` (dead copy deleted)

### P1 — Monolith / shared core ★
- [x] `capabilities/base.py`: ToolResult, CallContext, Capability, ResultStore, ServiceClient (+ Provenance, Budget, BaseCapability)
- [x] Extend `core/tools.py` registry (namespacing, validation, collision protection, intent-subsetting)
- [x] `capabilities/datalab.py` (reference migration — 7 SQL/catalog tools) + `adapters/native/`
- [x] Parity: datalab capability path ≥ DataLabBench baseline — **65.6 ≥ 65.5** (flag ON); unit parity ✓ (see `PARITY.md`)
- [x] Retire inline path for the 7 migrated SQL tools (flag removed, inline methods deleted → agent.py 12,714→12,640, one implementation each)
- [~] Migrate remaining datalab tools — **22/≈25 done** (incl. `density_aggregate` tiling + `job_status`, both via CallContext-injected per-turn state). Left: `density_vetting`, `tiled_search`, `export_notebook`. Then families: alma / ads / vo / viz / resolve / files / spectra
- [ ] Extract `core/runner.py` (from `stream_response_api`)
- [ ] Extract `core/router.py` (from routing regexes)
- [x] Split `ui-pro/api/main.py` (4,368→~250 lines) → `api/routers/*` (17 modules) + `api/sse.py` + `api/serializers/*` (+ `deps.py`/`models.py`/`bootstrap.py`); 68-route parity, S4+S5 fixed, boot-verified (see change log 2026-07-09 api-split)
- [ ] **Gate:** `agent.py` & `main.py` < ~1,500 lines; benchmark ≥ baseline

### P2 — MCP path
- [ ] Fix client bridge (long-lived loop + run_coroutine_threadsafe)
- [ ] `adapters/mcp/server.py` (FastMCP over capabilities)
- [ ] Mount MANNA (flag per family); migrate datalab_* calls
- [ ] Per-session MCP auth (tokens off `os.environ`)
- [ ] **Gate:** same capability callable native + MCP; MANNA datalab ≥ baseline

### P3 — Native path improvements
- [ ] Native adapter thin over capabilities
- [ ] result_id handles registry-wide (kill thread-local LAST results)
- [ ] Intent-based tool subsetting
- [ ] Delete `exec()` user tools

### P4 — Testing
- [ ] Per-capability unit tests
- [ ] Adapter parity tests
- [ ] Benchmark extensions (fabrication penalty, subtask tracing, variance)
- [ ] CI runs capability tests + scorer self-tests

### P5 — Cleanup
- [~] Delete dead modules — **done:** `agents/`, `core/workflow_store.py`, `core/model_council.py`, `core/result_cache.py` (grep-verified zero live importers; `result_cache` also unwired from `core/conductor.py:36,306`, its `main.py:4344` reader is hasattr-guarded; app boot + unit suite clean). **Pending — need `core/agent.py` edit (main-line owns):** `agent_pool`, `context_manager`, `session_memory`, `health_monitor`. **Blocked — live CI importer:** `config/settings.py`+`config/__init__.py` (`.github/workflows/ci.yml:75` `from config import settings`; remove that line first).
- [ ] Consolidate: one TAP engine, one ADS client, one plotting service, one SIMBAD resolver
- [ ] pydantic-settings config (replace 179 os.getenv)
- [ ] Repo slimming

### P6 — Docs
- [ ] Keep this `docs/v2/` set current
- [ ] Generate tool-registry doc + diagrams in CI
- [ ] Fix tool-count drift; add CONTRIBUTING.md

## Change log

| Date | Who | Change | Benchmark delta |
|---|---|---|---|
| 2026-07-11 | worktree-consolidation session | **Worktree reconciliation → beta is the single branch.** Audited all 9 worktrees + 12 local branches: every branch is 0 commits ahead of `beta`; the only unmerged work was uncommitted deltas inside 6 worktrees. A 4-agent adversarial subsumption review confirmed 5 of 6 were stale earlier iterations of work already re-landed in evolved form (content-safety tests, gpt-oss parse-error recovery + its test file, 140+ tool-count docstrings, dust-extinction files, DataLabBench results). One real gap found and ported from `frosty-kepler`: the **explicit `morphology` cut on the one-shot diagram tools** (dropped, not evolved, when point_sources was reimplemented) — now threaded through `datalab_orchestration.color_color_diagram`/`color_magnitude_diagram` (`_diagram_dataframe`), `ColorColorDiagramInput`/`ColorMagnitudeDiagramInput`, and both agent tool schemas; explicit cut overrides `point_sources`, suppresses the CCD stars/galaxies auto-split, and is surfaced verbatim in the result (`morphology` key). Supports op/value, `between`, and `in` cuts via `build_catalog_predicates` (value_cuts could not express the latter two). Also ported wizardly-saha's `logger.error(..., exc_info=True)` traceback logging at the Responses API fall-through. +3 unit tests (morphology-overrides-point_sources, CCD split suppression, schema exposure); suites green: datalab_p2 + datalab_capability (75✓), capabilities_base + datalab_p0 + p0_stabilization + response_api_error_recovery (57✓). Worktrees/branches are now safe to delete (all content subsumed). No commit/push. | n/a — additive tool option; benchmark parked. |
| 2026-07-10 | security session (Fable 5) | **S6 COMPLETE — JWT out of localStorage (frontend flip) + throttle HTTP semantics.** Ran as `@cx-build` (gpt-5.6-terra/ultra): Terra's consult (8.09M tok) produced 10 verified findings + 8 questions; the headless plan gate ruled on all (server-authoritative init, no-XFF-by-default, IP bucket never resets, trip=401/locked=429, `/me` NOT extended — FE fields optional); **Codex hit its usage limit at the build step → Claude implemented solo per the standing fallback, following spec+ruling.** Landed: `auth-store.ts` rewritten (persists only `user`, v1 `migrate` strips legacy JWTs, `/me` bootstrap with auth-generation guard, `clearAuth`, logout POST); **54 backend fetches** get `credentials:"include"` (script-verified ledger = Terra's count); ~35 `!token` guards/dep-arrays → `isAuthenticated` across SettingsModal/Sidebar/ChatArea/ChatMessage/DataTableCard/spectral/workbench; `page.tsx` init gate (no modal flash); 429+`Retry-After` for locked logins (per-user AND new per-IP limiter, `QUASAR_LOGIN_IP_*`, XFF only under `QUASAR_TRUST_XFF=1`); CORS `expose_headers=["Retry-After"]` (**browser-found defect** — TestClient can't see CORS). `scripts/local/launch_backend_local.py` for preview-tool dev boots. Tests 29✓ in suite (58✓ with auth/quota/P0); `tsc` 0. **Browser-verified:** login 1@1/1 → httpOnly cookie (JS-invisible), localStorage token-free, reload stays authed, logout clears, 429 countdown readable. Sol/ultra post-review in flight. No commit/push. | n/a — no benchmark-path code touched. |
| 2026-07-09 | this session | **P1 — migrate the two request-scoped-state datalab tools + retire the SQL helpers.** `density_aggregate` (auto-tiling + per-turn sync-timeout table set) and `job_status` (per-turn poll counter → `stop_polling` nudge) → `capabilities/datalab.py`. **The pattern:** the agent lazily owns the mutable per-turn objects (`_get_datalab_agg_timeout_tables` / `_get_datalab_job_poll_counts`), `_datalab_ctx_provider` injects them into `CallContext.services` **re-reading the attribute on every tool call** so the per-turn rebind in `stream_response_api` (unchanged) is picked up — capabilities mutate the injected object and never touch `self`. Deleted the 2 inline methods + `_execute_datalab_sql` + `_datalab_query_summary` (grep-proven zero remaining callers; `_datalab_error`/`_datalab_fit_rows`/`_datalab_coordinates` still live). Registrations swapped `function=` only → `_datalab_tool_fn("...")`. Capability `description` strings copied verbatim from the inline `Tool(...)` so the direct `build_tool()` surface matches the registry's. `agent.py` 12,345→**12,208**; `CAPABILITIES` 20→22. **+23 unit tests** (72→95 in the gate suite): budget-ordering (first timeout `max_seconds=None`, repeat `120.0`), `radius_deg == 2.0` boundary, skip-sync short-circuit, narrow-cone no-tile, null-`mode` passthrough, sentinel-identity of the non-tiling `ToolResult`, real terminal statuses (`succeeded`/`failed`/`canceled`), per-job counters, and two agent-wiring regressions (shared object identity; the per-turn reset is picked up). Boot-verified: real `QuasarAgent()` registers both tools, `POST /api/auth/login` → 200 + token. **Ran as `@cx-build` (gpt-5.6-terra/ultra); Codex's consult landed, then it hit its usage limit before the build step — Claude implemented solo per the standing fallback, folding in the consult's valid findings** (exact legacy descriptions; counter fetched only inside the queued/running branch; terminal statuses are not `completed`). **Codex could not cross-review, so a 13-agent adversarial parity review ran in its place** (6 lenses → per-finding refutation): 7 raised, 4 refuted (incl. both "medium"s — verifiers proved the `>= 2.0` gate and the relocated summary dict are byte-identical to HEAD), **3 confirmed → 1 fixed, 2 accepted**. Fixed: `all_sky` typed `bool` let pydantic coerce `"false"/"0"/"no"/"off"` → `False`, flipping the builder's truthiness gate vs legacy → retyped `Any` + 11 parity tests (same hazard/fix as `expert_ack`); also repaired a pre-existing test whose docstring described a fake `raise` that never existed. Accepted (pre-existing, consistent with the 20 already-benchmarked tools): pydantic `ValidationError` dicts differ in wording from legacy `TypeError`/`ValueError` dicts; `extra="ignore"` tolerates hallucinated kwargs. Latent legacy bug logged as **C18** (string `"false"` triggers an unbounded all-sky scan) — preserved verbatim, fix is benchmark-gated. **Whole-suite regression control:** `pytest tests/unit` fails 30 WITH my tests and **31 WITHOUT** them — zero failures unique to this change; `test_datalab_capability.py`/`test_capabilities_base.py` green in both. Beyond the 8 documented pre-existing failures, `test_sky_monitor.py` (12) + `test_s3_s6_security.py` (10–11, varies with ordering) fail **only in whole-directory runs** and pass in isolation (14✓ / 37✓) → **cross-test state pollution, pre-existing, not this lane** (see "Testing debt" below). No commit/push. | Benchmark parked by user; unit + boot verified. |
| 2026-07-09 | api-split session | **P1 — split `ui-pro/api/main.py` (4,368 lines) into a package.** Routes → `api/routers/*` (17 modules); the SSE chat state machine → `api/sse.py`; UI serialization (per-archive column maps, CADC VOTable XML, matplotlib FITS render) → `api/serializers/{data_card,cadc,fits}.py`; shared singletons/helpers → `api/deps.py`; pydantic request models → `api/models.py`; env shims + self-locating `sys.path` → `api/bootstrap.py`. `main.py` is now ~250 lines (app + middleware + static mounts + startup/shutdown + exception handler + `include_router`); `_build_data_card_event` re-exported for the importlib-loading tests. **S4:** CORS wildcard (`*.vercel.app`/`*.onrender.com` + `allow_credentials`) → explicit allowlist (`quasarassistant.com`/`www` + `QUASAR_CORS_ORIGINS` env + a tight localhost regex). **S5:** auth (`Depends(get_current_user)`) added to `/api/conductor/traces`, `/api/conductor/traces/{trace_id}`, `/api/proposals/review`; frontend `reviewProposal` now sends the Bearer token. **Verified:** exact 68-route parity (identical paths+methods), auth posture identical except the 3 S5 additions; boot `QUASAR_ENV=development :8000` → real `/api/auth/login` + `/api/chat` SSE completion (`run_meta`→tokens→`usage`→`[DONE]`) + S4/S5 behavioral checks (evil.vercel.app blocked, traces/proposals→401); 19 importlib-load unit tests pass; pyflakes clean; **5-agent adversarial parity review → 0 confirmed divergences**. Caught+fixed one extraction miss (`stdlib_queue` import absent in `sse.py`, found via boot). Composes cleanly with the concurrent S6 cookie-auth layered on the same routers. No commit/push. | n/a — behavior-preserving; DataLabBench path untouched. |
| 2026-07-09 | security session | **S3 + S6 auth hardening** (scoped S3 + S6 only; S4/S5 owned by another session — `ui-pro/api/main.py` and `core/agent.py` **untouched**). **S3:** removed the hard-coded `1@1` admin (`services/admin_access.py` `DEFAULT_ADMIN_EMAILS` now `set()`) → admin now via the `ADMIN_EMAILS` env allowlist **or** a new `users.role='admin'` column (TTL-cached DB lookup so the per-request quota path stays cheap; the local test seed carries `role='admin'`). `is_admin_email`/`is_quota_exempt_email` keep their signatures, so the `main.py` call sites needed no change. **S6:** login now uses `hmac.compare_digest` (was `==`) with byte coercion (`auth.py:~275`); added per-identity **login rate limiting** (`services/login_rate_limit.py`, memory-bounded LRU + sweep). **S6 cookie = STAGED:** `services/auth_cookie.py` httpOnly helper is ready but intentionally **not wired** (needs `main.py` + S4 CORS — see handoff below); frontend stays on Bearer. Tests: `tests/unit/test_s3_s6_security.py` (18✓; 48✓ with auth/quota/P0 suites). **15-agent adversarial review → 3 confirmed defects fixed** (rate-limiter memory-exhaustion DoS, cookie `Secure`-vs-`SameSite=None` ordering, TTL-cache coverage gap) + `set_user_role` cache invalidation. E2E: real `/api/auth/login` `1@1/1` → 200 + admin; throttles after N fails. No commit/push. | n/a — no benchmark-path code touched. |
| 2026-07-09 | this session | **P5 cleanup (partial): dead-code deletion.** Removed `agents/` (8 files), `core/workflow_store.py`, `core/model_council.py`, and `core/result_cache.py` (also unwired from `core/conductor.py:36,306`; `main.py:4344` reader is hasattr-guarded, left untouched) — all grep-verified zero live importers. Proof: `from api.main import app` boots clean with all four confirmed non-importable; unit suite shows no new failures (8 pre-existing unchanged). Deferred (need `agent.py` edit, main-line owns): `agent_pool`/`context_manager`/`session_memory`/`health_monitor`. Blocked: `config/*` (live CI importer at `ci.yml:75`). No commit/push. | n/a — no benchmark-path code touched. |
| 2026-07-09 | this session | **P1 — migrate + retire datalab orchestration/color/job tools.** `confirm_sky_area`, `color_color_diagram`, `color_magnitude_diagram` (image), `job_results`, `job_cancel` → `capabilities/datalab.py`; job service injected via CallContext; `datalab_orchestration` imported (pure). Deleted 5 inline methods. `agent.py` 12,428→**12,345**. +5 unit tests. **20/≈25 datalab tools capability-backed.** | Benchmark deferred (quota); unit + boot verified. |
| 2026-07-09 | this session | **P1 — migrate + retire datalab SIA imaging tools.** `image_cutout` (incl. band-substitution), `color_image`, `cutout_grid` → `capabilities/datalab.py`; image service + coordinate resolver injected via CallContext; image wrapper passes captions via a private `_caption` key (no LLM leak). Deleted the 3 inline methods. `agent.py` 12,512→12,428. +4 unit tests (SIA logic + band-sub + end-to-end wrapper). **15/≈23 datalab tools now capability-backed.** Investigated Track B's agent.py dead-module handoff → found `session_memory` (3 live calls) + `agent_pool` (Conductor) are behaviour-affecting, not clean wiring removals → deferred until the benchmark gate is back. | Benchmark deferred (quota); unit + boot verified. |
| 2026-07-09 | this session | **P1 — migrate + retire datalab plot tools.** Designed the image-emission mechanism (`_datalab_image_tool_fn`: capability returns the raw plot result; the wrapper applies `_datalab_attach_image_result` for the UI card + base64 stripping at the adapter boundary). Migrated the 5 result_id-plot tools (`catalog_scatter`/`sky_density_map`/`period_fold`/`sed_plot`/`lss_wedge`) to `capabilities/datalab.py` (`PLOT_CAPABILITIES`; SED gets the SVO client via CallContext); deleted the 5 inline methods. `agent.py` 12,640→12,512. +4 unit tests (image plumbing + SVO injection). | Benchmark deferred (quota); unit + boot verified. |
| 2026-07-09 | this session | **P1 — retire datalab inline path.** Removed the `QUASAR_USE_CAPABILITY_DATALAB` flag; the 7 SQL tools register via `_datalab_tool_fn` (capability-backed, unconditional); deleted the 7 inline `_datalab_*` methods. `agent.py` 12,714→12,640 (first net shrink). Contract tests redirected through the registry; real-`__init__` boot verified. | No re-run needed — runtime identical to the flag-ON path already at **65.6 ≥ 65.5**. |
| 2026-07-09 | this session | **P1 shared core — reference migration.** New `capabilities/` layer (base contracts + datalab family), `adapters/native/`, extended `core/tools.py` registry; 7 Data Lab SQL tools routed through capabilities behind `QUASAR_USE_CAPABILITY_DATALAB` (default OFF). 23 new unit tests. Adversarial review (9 agents): expert-ack + client-fetch parity fixes applied. | **Flag-ON 65.6/100 (74.2 auto) ≥ 65.5/100 baseline** — parity confirmed (`results/p1_datalab_capability_gpt-4.1/PARITY.md`). |
| 2026-07-09 | this session | **P0 stabilization + adversarial-review round.** Fabrication→typed errors (C1 analysis.py, C2 ads_client.py **+ dead-duplicate services/ads_service.py + guard on legacy `QuasarAgent.search_papers`**, C3 search.py + `search_by_target`/multi/per-band + `_search_by_position`); RCE guards behind default-OFF flags (S1, S2); cheap wins (C5 RAG gate, C6 rank_bm25, C7 recovery SLA/routing + DECOMPOSE arity, C9 DAGCache reconciliation w/ word-boundary substitution + fuzzy/proper-name guard, C11 dup method). A 13-agent review workflow found 6 confirmed defects — all fixed. `tests/unit/test_p0_stabilization.py` (19✓). | **Baseline 65.5/100** (72.3 auto) at `650c354` gpt-4.1. Fixes don't touch the datalab capability path → no datalab regression. |
| 2026-07-08 | prior session | Created `docs/v2/` handoff set (11 files). No code changes. | n/a |

## Testing debt — `pytest tests/unit/` is not isolation-clean (discovered 2026-07-09, main line)

`.github/workflows/ci.yml:57` runs `pytest tests/unit/ -v --tb=short -q` — exactly the invocation
that trips this, so **CI is affected**. 22 tests pass alone and fail together:

- **`test_s3_s6_security.py` (10–11, count varies with collection order) — diagnosed.**
  `services/login_rate_limit.py` holds a process-global singleton (`_default_limiter`, ~:146)
  whose per-username/per-IP failure counters are never reset between test modules. Earlier tests
  exhaust the failed-login budget, so e.g. `test_no_cookie_on_failed_login` (:579) gets a **429**
  where it asserts **401**. Fix: autouse fixture resetting/replacing the default limiter per test
  (`LoginRateLimiter.reset` exists at ~:136), or make the singleton injectable.
- **`test_sky_monitor.py` (12) — NOT diagnosed.** Dedup-shaped symptom (`n_new == 0` where 200/1
  expected). Each test builds its own `SkyMonitorService(db_path=tmp_path/...)`, so it is *not* a
  shared sqlite file, and `services/sky_monitor.py` has no module-level cache. Suspect a leaked
  `os.environ` write (bare assignment, not `monkeypatch`) or leaked state in a faked collaborator.
  Bisect with `pytest tests/unit/<other>.py tests/unit/test_sky_monitor.py`.
- **The whole-directory run also never exits** after printing its FAILED summary (a non-daemon
  thread is never joined) → CI would hang rather than report.

Verified pre-existing, not caused by the P1 capability work: with the main line's test files
excluded the failure count goes **up** (30 → 31), and no failure is unique to their presence.
Documented baseline failures remain `test_datalab_p1` ×3, `test_issue_report_service` ×4,
`test_content_safety` ×1.

## Decisions log (append as decisions are made)

| Date | Decision | Rationale |
|---|---|---|
| 2026-07-08 | Build MCP first, keep BOTH paths; monolith is #1 priority | MCP breaks the monolith + MANNA already being built; native path needed for latency/trust/continuity |
| 2026-07-08 | Monolith split == shared-core extraction (same work) | Capabilities layer is both the de-monolith and the dual-path foundation |
| 2026-07-08 | Migrate `datalab` family first | Already uses result_id handles; has test contract; cleanest 1:1 with MANNA |
| 2026-07-09 | RCE surfaces gated behind default-OFF env flags (`QUASAR_ENABLE_USER_TOOL_EXEC`, `QUASAR_ENABLE_MCP_STDIO`), not deleted | P0 must keep prod safe without breaking local dev; deletion of `exec()` user tools is P3. http/SSE MCP stays allowed (MANNA consumption model is unaffected). |
| 2026-07-09 | C3 fixed via `.attrs["quasar_error"]` tag (not exceptions) in P0 | Backward compatible with ~20 `.empty` call sites; the full per-handler typed-error contract is deferred to the P1 `ToolResult` migration (avoids a risky broad refactor in P0). |
| 2026-07-09 | C7 routes recovery through `_execute_node` + SLA; inline double-recovery removed | Unifies the recovery and non-recovery paths (both now use the full node executor with model routing/sandbox/SLA). ⚠️ DataLabBench does not exercise the Conductor recovery path — needs a dedicated complex-query smoke test (P4). Verified offline via `test_p0_stabilization::test_c7_*`. |
| 2026-07-09 | C11: deleted the *dead* (shadowed) `_filter_results` at agent.py:8397, kept the live one at 10147 | Zero behaviour change (Python already used 10147). The richer aliasing/`pd.to_numeric` logic from the deleted copy was NOT promoted — that would be an unbenchmarkable behaviour change; revisit during the ALMA family migration. |
| 2026-07-09 | **P1 migration is parity-first: capability sets `ToolResult.native` = the exact legacy dict; the agent REUSES the legacy tool schema/description at wire-time** | Guarantees byte-identical native output + zero LLM-facing schema drift, so the migration is measured purely by "≥ baseline". Canonical `ToolResult` fields are still populated for the future MCP adapter. `native` is a documented migration aid; new capabilities won't need it. |
| 2026-07-09 | Migrated only the Data Lab **SQL/catalog sub-family** (7 tools) as the reference; density_aggregate/tiling + imaging/plotting deferred | Establishes the full end-to-end pattern (pure builders + injected client/store + typed errors + result_id handles) with the lowest-risk, highest-parity subset. Remaining datalab tools follow the same template next. |
| 2026-07-09 | Kept capability input `extra="ignore"` (review finding B) rather than matching legacy's TypeError-on-extra-kwarg | The capability path is strictly more robust (tolerates hallucinated kwargs; malformed args yield a clean typed validation error instead of a cryptic downstream one) while preserving the success/failure outcome. Benign, documented divergence — not a regression. |
| 2026-07-09 | **S4:** explicit CORS allowlist + `QUASAR_CORS_ORIGINS` env override; localhost kept via a tight regex; `allow_credentials=True` preserved | Removes the credentialed-CORS hole (any attacker-registered subdomain on `*.vercel.app`/`*.onrender.com` could make credentialed cross-origin calls) while preserving prod (`quasarassistant.com`/`www`) and dev (`localhost`/`127.0.0.1:300x`). Operators re-add a specific deploy origin (Vercel preview / raw onrender URL) via env — never a wildcard. |
| 2026-07-09 | **S5** scoped to `/api/conductor/traces`, `/api/conductor/traces/{id}`, `/api/proposals/review` only; `/api/conductor/{health,dag-cache}` left public | Matches the OPEN_ISSUES S5 definition exactly ("`traces*`, `proposals/review`"); health/dag-cache were not named. Frontend `reviewProposal` (`ui-pro/src/lib/api.ts` + `ChatArea.tsx`) updated to send the Bearer token so the Red-Team-TAC feature keeps working under the new gate. |
| 2026-07-09 | The split preserves the `_compute_demographics` pandas-scope latent bug **verbatim** (pandas stays function-local in `_build_data_card_event`, never hoisted to module scope) | A pure refactor must be byte-identical: hoisting pandas would make the currently NameError-swallowed `observationYears`/`resolutionBins` branches start populating data cards — an observable output change. Fixing that latent bug is a separate, benchmark-gated task (documented in `api/serializers/data_card.py`). |
| 2026-07-09 | `api/bootstrap.py` self-locates `sys.path` (repo-root first, ui-pro appended) + runs the Windows/py3.13 shims; every package module imports it first | Makes `api.*` importable regardless of CWD or load mechanism (uvicorn AND the `importlib.spec_from_file_location` tests that load `main.py` by path), and guarantees the `pwd`/`cgi`/stdout shims run before any `from services.*` import. ui-pro is *appended* (low priority) so it can never shadow a repo-root package (`core`/`services`/`tests`). |
| 2026-07-08 | **MCP built+tested LOCALLY, deploy DEFERRED; native tools stay the production path on Render (2 GB)** | No MCP host available; co-locating MCP on the 2 GB Render box doubles RAM and OOMs. MCP relieves memory only on a separate host. Monolith fix (P1) is native refactoring and needs no hosting. Consume MANNA (NRAO-hosted) rather than self-host. |
| 2026-07-09 | **S3: admin via `ADMIN_EMAILS` env allowlist + a DB `users.role` column; NO compiled-in admin.** The disposable local seed sets `role='admin'`. | Removes the permanent `1@1` admin without breaking local dev. `is_admin_email` stays same-signature (now `env OR db-role`), so no `main.py` edit was needed. The DB lookup is TTL-cached (default 60s, `QUASAR_ADMIN_ROLE_CACHE_TTL`) because `is_quota_exempt_email` runs on the per-request quota path; `set_user_role` clears the cache so grants/revokes are immediate. |
| 2026-07-09 | **S6 rate limiting is per-username, in-memory, per-worker ("basic").** | Meets the "basic rate limiting" ask without a shared store. Known tradeoffs (documented in the handoff): (a) targeted account-lockout DoS is possible for a known username — inherent to username-keyed lockout; an IP/edge limiter is the real fix; (b) per-worker state means N workers ⇒ N× the effective threshold. Memory is hard-bounded (LRU cap) so the unauthenticated endpoint can't be used to exhaust RAM. |
| 2026-07-09 | **S6 httpOnly-cookie migration STAGED, not wired; frontend kept on Bearer.** *(SUPERSEDED 2026-07-10 — fully wired + flipped, see below.)* | Setting the cookie requires editing the login/register/google handlers + `get_current_user` in `ui-pro/api/main.py` (owned by another session), and it only functions cross-origin once S4's CORS `allow_credentials`+exact-origin lands. Wiring here would collide with that session. Shipped a ready `services/auth_cookie.py` + a handoff instead. |
| 2026-07-10 | **S6 flip: server-authoritative auth init.** `isInitialized` stays false until `GET /api/auth/me` (with `cache:"no-store"`) resolves; persisted `user` is display data only; an auth-generation counter discards stale `/me` responses; 401s mid-session use local-only `clearAuth()`. | Kills three verified races (plan-gate ruling Q2/Q4): auth modal stuck open on valid-cookie/empty-storage, a slow `/me` resurrecting a logged-out session, and a delayed 401 leaving the UI "logged in" while every call fails. Offline/network errors deliberately do NOT log the user out. |
| 2026-07-10 | **S6 throttle semantics: trip=401, locked=429 (+`Retry-After`, CORS-exposed); IP limiter keyed on the socket peer, XFF only under `QUASAR_TRUST_XFF=1`; the IP bucket NEVER resets on success; only invalid-credential failures consume IP budget.** | Ruling Q5/Q6: the tripping attempt's creds were genuinely wrong (401); only lockout state is a throttle (429). Blind XFF trust would let attackers spoof their limiter key (and lock out arbitrary IPs); Render deployments set `QUASAR_TRUST_XFF=1` since the socket peer there is the ingress proxy. No-reset-on-success stops a password-sprayer erasing its own history with one valid credential. `Retry-After` isn't CORS-safelisted → `expose_headers` in `main.py` (found only by real-browser verification). |
| 2026-07-10 | **S6 `/api/auth/me` NOT extended with `auth_provider`/`picture_url`; the frontend User fields are optional instead.** | Ruling Q3: those fields aren't in the JWT payload and sourcing them would add a DB lookup on every bootstrap. `verifyAuth()` merges over the persisted user and never defaults the provider (a storage-cleared Google user must not be mislabeled "local"). |
| 2026-07-10 | **`_client_ip` reads X-Forwarded-For from the RIGHT (`parts[-hops]`, `QUASAR_TRUSTED_PROXY_HOPS`, default 1), never `split(",")[0]`.** Header shorter than the hop count → fall back to the socket peer. | Found by the post-implementation adversarial review (MAJOR). Proxies **append** the peer they received from, so XFF is `<client-supplied…>, <real peer>`: the leftmost element is fully forgeable. Trusting it under `QUASAR_TRUST_XFF=1` (the intended Render config) let an attacker (a) mint a fresh limiter key per request — bypassing the per-IP bucket entirely and flooding its 4096-key LRU — and (b) **lock out any chosen victim IP** for the lockout window by spending the bucket under a forged key. The `QUASAR_TRUST_XFF` gate only decides *whether* to read XFF, not *which element* is trustworthy. A regression test now pins the rightmost-trust behaviour (the original test had pinned the vulnerable one). |
| 2026-07-10 | **Auth fails closed on an invalid *explicit* Bearer — `bearer or cookie`, NOT try-each-in-turn.** (Reverses a mid-session change of mine.) | Sol's review (CX-02) caught that "try each credential" would let a caller with a low-privilege/expired Bearer be silently executed as whatever identity the browser cookie carries — a confused-deputy escalation. Fail-closed is correct; the stale-Bearer UX edge it "fixes" never occurs post-flip because the frontend stops sending Bearer at all. |
| 2026-07-10 | **BLOCKER (CX-01): `register_user` now rejects a duplicate email (case-insensitive, both columns).** | Admin is keyed on email in BOTH the `ADMIN_EMAILS` allowlist and the DB-role lookup, and registration only checked `username` uniqueness. So a new account could register under an existing admin's email and its JWT (which carries that email) inherited admin. This is the highest-severity finding of the whole S3/S6 effort; found only by the post-implementation review, not by any test or the browser pass. |
| 2026-07-10 | **CX-04 resolved by the SECURE reading, overriding the plan-gate ruling's literal wording.** | The ruling said "parse the first XFF hop"; the *first/leftmost* element is client-forgeable (proxies append). I key on the rightmost trusted hop (`parts[-hops]`). Following the ruling's letter would reintroduce the forgeable-key bypass + targeted-IP-lockout the review flagged. Documented as an intentional divergence for the ruling's intent (a trustworthy client IP). |
| 2026-07-10 | **CSRF: `CookieCsrfMiddleware` Origin check LANDED with the cookie flip (not deferred).** For unsafe methods, a request carrying the auth cookie whose `Origin` is not allow-listed → **403**. Absent `Origin` is allowed (non-browser clients can't be CSRF victims); Bearer-only clients are untouched. Added *before* CORS so CORS stays outermost and the 403 still carries CORS headers. | Moving the JWT into an httpOnly cookie **removed XSS exfiltration but introduced a CSRF surface Bearer never had** — browsers attach the cookie automatically, and CORS governs reading the *response*, not sending the *request*. Prod is cross-site → `SameSite=None`, so SameSite alone does not close it. Verified-exploitable preflight-free writes: `/api/chat/upload`, `/api/personalization/upload` (**poison a user's private RAG corpus**), `/api/proposals/review` (all multipart), and `/api/feedback` (`await req.json()` ignores Content-Type → `text/plain` skips preflight). **Negative control:** with the middleware disabled, a cross-site `POST /api/chat/upload` carrying the cookie returned **200**; with it, **403**. Shipping the cookie without this would have been a net-sideways security trade. |

## Open decisions still needed (from OPEN_ISSUES D1–D7)

- [ ] D1 FastMCP version target (3.x vs ≥2.0) — confirm with MANNA repo
- [ ] D6 Is the MANNA repo public / consumable yet — verify with Dan Gause / github.com/nrao
- [ ] D2 Result-store ownership when capability runs server-side in MANNA
- [ ] D4 Token passthrough vs OAuth proxy
- [ ] **D8 (NEW) Where to eventually host MCP for production** — options: Cloud Run / Modal (scale-to-zero), Hetzner VPS, or dlai1. Not blocking; native tools serve production until decided. Render (2 GB) is NOT a viable MCP host.

## S6 httpOnly-cookie migration — COMPLETE (backend + frontend, browser-verified)

> Both halves are done and browser-verified. The step-by-step below is kept as
> the record of what landed and the security rationale (SameSite/CSRF, XFF).

**Backend: DONE + verified.** The auth code was split out of `main.py` into
`api/routers/auth.py` + `api/deps.py` (Track B's P1 split), and **S4 CORS is
already done** there (exact origins + `allow_credentials=True` + a
`http://localhost:300[0-9]` dev regex). The cookie is wired in:

- `services/auth_cookie.py` — `set_auth_cookie` / `clear_auth_cookie` /
  `read_auth_cookie` (+ `cookie_params`/`cookie_secure`/`cookie_samesite`).
- `api/routers/auth.py` — `set_auth_cookie(response, token)` on
  `/api/auth/login`, `/register`, `/google`; new `POST /api/auth/logout` →
  `clear_auth_cookie`. Token still returned in the body (Bearer transition).
- `api/deps.py` — `get_current_user` and `_resolve_optional_user` accept the
  cookie when no `Authorization: Bearer` header is present.
- `api/sse.py` + `api/routers/chat.py` + `api/routers/analytics.py` — the
  cookie token is threaded into the optional-user resolution so the chat
  personalization / quota path works cookie-only.
- Verified via TestClient E2E: cookie-only auth reaches `/api/auth/me` and
  `/api/conversations`; Bearer still works; no-auth → 401; logout → 401.

Dev note: `localhost:3001`→`localhost:8000` is **same-site** (SameSite ignores
port), so the default `SameSite=Lax` cookie is sent — no Secure/None needed in
dev. Production (frontend vs. API on different sites) uses `SameSite=None; Secure`,
auto-resolved by `auth_cookie` from `QUASAR_ENV`.

**Frontend: REMAINING (deferred).** This is the actual "JWT out of localStorage"
win and it's a real refactor, not a one-liner — the app uses `token` *presence*
as its auth signal in ~20 guard sites:

1. In `ui-pro/src`, add `credentials: "include"` to every authenticated API
   fetch — centralise via a small `apiFetch` wrapper in `src/lib/api.ts` and
   the direct fetches in `AuthModal.tsx` / `SettingsModal.tsx` / etc. (so the
   browser stores the Set-Cookie and sends it back).
2. In `src/lib/auth-store.ts`, stop persisting `token` (persist only the
   non-secret `user`); derive `isAuthenticated` from `user`; have `logout()`
   call `POST /api/auth/logout`.
3. Migrate the `if (!token) return;` guards → `!isAuthenticated` (or `!user`)
   in `app/spectral-lines/page.tsx` (~10), `app/workbench/[sessionId]/page.tsx`
   (~5), `components/Sidebar.tsx` (~3), plus `ChatArea.tsx`/`ChatMessage.tsx`.
   Otherwise, after a reload (token gone, cookie valid) the app looks
   logged-in but silently stops loading conversations/workbench/spectral jobs.
4. Verify in a real browser: login → reload stays authed via cookie →
   conversations/workbench/spectral load → logout clears.

**Also recommended (not done here):** the login rate limiter is per-username /
per-worker; add an IP-based or edge limiter (and consider HTTP 429 instead of
401 for throttled logins).
